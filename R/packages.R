
# data organization
require(openxlsx)
require(here)
require(reshape)
# phylogeny

library(phytools)
library(geiger)
require(picante)

# trait simulation
require(mvMORPH)
require(parallel)

# functional diversity
require (FD)

#  fishtree 
require("fishtree")

# plot
require(ggplot2)
require(gridExtra)

# analyses
require(brms)
require(emmeans)


# table
library("sjPlot")
library("sjmisc")
library("sjlabelled")

# maps & space
require(rnaturalearth)
require(rnaturalearthdata)
require(ggplot2)
require(gridExtra)
require(ggrepel)
require (scatterpie)
require(sf)
require(rgeos)
